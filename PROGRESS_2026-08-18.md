# 多相机时间戳同步校验 —— 进度总结

**日期：2026-08-18**

硬件：3 台 Orbbec 相机（2× Gemini 305g + 1× Gemini 335Lg），GMSL2/FAKRA 接口，外部 PWM 硬件触发（`OB_MULTI_DEVICE_SYNC_MODE_HARDWARE_TRIGGERING`）。

目标：验证三台相机在同一触发沿下，出帧时间戳是否对齐。

---

## 一、今天已解决的问题

### 1. 三台相机无法同时启动、帧数随机错开 —— ✅ 已修复

- **根因**：原来 PWM 触发是在另一个终端手动 `echo 10 > .../framerate` 跑的。程序启动时触发已经在跑，三台相机的 `start()` 内部耗时不同、**先后加入触发流**，导致帧数错开、帧号无法一一对应。
- **修复**：程序接管触发开关，改造成"**先 arm、再触发**"流程：
  1. 启动即写 `0` 关触发（清空旧触发流）；
  2. 枚举 / 配置 / 三台 `start()` 就绪（此时无触发、不出帧）；
  3. 等待 300ms settle（相机进入可触发状态）；
  4. 写 `N` 开触发 ← **发令枪**，三台从同一触发沿开始出帧；
  5. 采集 N 秒；
  6. 先关触发，再停 pipeline。
- **新增参数**：`--trigger-hz=N`（>0 时程序自动写 `/sys/kernel/debug/gpio_trigger/framerate`）。

### 2. 帧数按设备索引递减（104/101/98）—— ✅ 已修复

- **根因**：停止顺序 bug。原来先逐个 `stop()` pipeline（逆序 2→1→0），**之后**才写 `trigger=0`。`stop()` 阻塞期间触发一直在跑，后 stop 的相机就多收几帧，造成按设备索引递减的固定帧数差。
- **修复**：`collectFrames()` 里改为**先关触发**（`writeTriggerFramerate(0)`），再逐个 stop。
- **验证**：三台 Depth 帧数从 `104/101/98` → **`99/99/99`**（完全拉平）。

### 3. `stop()` 偶发卡死 —— ✅ 已修复（待重传编译验证）

- **根因**：OrbbecSDK 的 `Pipeline::stop()` 在硬件触发模式下偶发阻塞——其内部收帧线程在等下一个触发沿，但触发已被我们关成 0，于是 `stop()` 一直等、阻塞主线程，导致后面的"打印帧数 + 写 CSV"全部不执行。
- **修复**：
  1. `stop()` 放到 `detach` 后台线程，主线程不 join，直接继续导出数据（保证 CSV 一定能落地）。
  2. `signalHandler` 恢复默认处理，卡死时第二次 Ctrl+C 可强制退出。
- **⚠️ 注意**：这处改动（`data_collector.cpp` + `main.cpp`）**尚未重传到远端编译**——用户上次运行输出仍是旧消息 `Pipelines stopped`（新代码应输出 `Pipelines stop requested (async)`）。

### 4. color 冷启动少帧 —— ⚠️ 已尝试 warmup 预热，未解决

- 加了 500ms 预热 + 清空预热帧的逻辑（`[WARMUP] color sensors warmed up, pre-trigger frames cleared`）。
- **结果**：color 仍比 depth 少 3~5 帧，预热无效。

---

## 二、当前未解决的问题

### 1. color 比 depth 少 3~5 帧（核心待解决）

5s@10Hz 实测：

| 设备 | Depth | Color | 差 |
|---|---|---|---|
| Dev 0 (305g) | 53 | 48 | -5 |
| Dev 1 (305g) | 53 | 49 | -4 |
| Dev 2 (335Lg) | 54 | 51 | -3 |

- **待定位**：color 丢帧是在**开头**（冷启动窗口 > 500ms）还是**中间**（持续丢帧）。
- 已给出 awk 诊断命令，等结果后再定修法：
  - 丢在开头 → 加长预热到 1~2 秒。
  - 丢在中间 → 关 color 自动曝光/白平衡（治本）。

### 2. Depth 帧数略超理论值（次要）

- 5s×10Hz 理论应出 50 帧，实测 Depth 53/53/54，多 3~4 帧。
- 疑点：触发频率并非精确 10Hz，或 335Lg 的 FPS boost（`OB_PROP_FPS_BOOST_BOOL`）在硬件触发模式下产生额外帧。
- 待 color 定位清楚后一并排查。

---

## 三、待办

1. 重传 `data_collector.cpp` + `main.cpp` 到远端，重新 `cmake .. && make`（async-stop 改动未编译）。
2. 在远端跑 awk 诊断，确定 color 丢帧位置（开头 vs 中间）。
3. 根据诊断结果，二选一：
   - 加长预热（若丢在开头）；
   - 关 color 自动曝光/白平衡（若丢在中间）。
4. 排查 Depth 多帧问题（触发频率精度 / FPS boost）。
5. 全部收尾后更新 `README.md`。

---

## 四、关键文件与改动位置

| 文件 | 改动 |
|---|---|
| `src/data_collector.cpp` | `writeTriggerFramerate()` 触发自动控制（先 arm 再触发）；stop 前先关触发；warmup 预热清空；stop 改异步 detach |
| `src/main.cpp` | `--trigger-hz=N` 参数；signalHandler 二次 Ctrl+C 强退 |
| `README.md` | 业务逻辑 + 执行指令 + 参数表（待同步最新改动） |

### 已验证的同步精度（参考）

- 305g ↔ 305g 硬件时间戳差约 113us（stddev ~6us）。
- 单相机内 Depth ↔ Color HW diff：305g 约 -57us，335Lg 约 -6us。
- 335Lg 相对 305g 有约 1.7ms 固定偏移。
- 结论：**三相机时间戳同步精度本身良好**，当前主要遗留问题是 color 帧数少 3~5 帧的采集完整性，不影响同步精度结论。
